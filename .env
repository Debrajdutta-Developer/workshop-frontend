# Base URL of the backend API (see workshop-backend/.env.example for the server's PORT).
# Used by src/lib/api.ts. Leave unset to default to http://localhost:5000 in dev.
VITE_API_BASE_URL=http://localhost:5000
